package ie.tudublin;

public class Colour {
	/* 
	 * Make a class called Colour that encapsulates the columns from a single row from the colours.csv file.
	 *  Make the r,g,b and value fields public and the colour (String) field private. 
	 *  Make accessor methods for the colour field. 
	 *  Make a constructor that takes initial values and assigns them to the fields. 
	 * Make an appropriate toString method
	 * */
	
	private string colour;
	public int r;
	public int g
	public int b;
	public int value;
	
	   public void colour(String colour)
	    {
	        this.colour= colour;
	    }
		public void init()
		{
			Colour colour = new Colour();
			Colour r = new Colour();
			Colour g = new Colour();
			Colour b = new Colour();
			Colour value = new Colour();
			init.Colour();
				
		}

	    public String toString()
	    {
	        return colour + ""\t" r + "\t" + g + "\t" + b + "\t" + value; 
	    }
}
